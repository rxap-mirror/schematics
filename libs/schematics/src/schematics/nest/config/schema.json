{
  "$schema": "http://json-schema.org/schema",
  "$id": "schematics-nest-config",
  "title": "Config",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The name of the project.",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "For which project should the config concept be added?"
    }
  },
  "required": [
    "project"
  ]
}
