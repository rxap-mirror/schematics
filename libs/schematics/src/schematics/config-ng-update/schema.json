{
  "$schema": "http://json-schema.org/schema",
  "$id": "schematics-config-ng-update",
  "title": "ConfigNgUpdate",
  "type": "object",
  "properties": {
    "project": {
      "type": "string",
      "description": "The project where the schematic should be added",
      "$default": {
        "$source": "projectName"
      },
      "x-prompt": "For which project the ng-update feature should be added?"
    }
  },
  "required": [
    "project"
  ]
}
